{
  "description": "Anti-AI-slop + attribution governance: enforces '🤖 Written by Claude, reviewed by <user>' on external posts, blocks slop filler, and blocks AI trailers in git commit messages",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__.*|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/attribution_hook.py"
          }
        ]
      }
    ]
  }
}
